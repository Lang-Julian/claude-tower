#!/usr/bin/env bash
# Deprecated shim. The real CLI is `tower`. Kept for back-compat with old aliases.
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "claude-control is deprecated — use \`tower\` instead." >&2
exec "$DIR/tower" "$@"
